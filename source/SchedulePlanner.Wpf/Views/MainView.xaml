<UserControl x:Class="SchedulePlanner.Wpf.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:SchedulePlanner.Wpf.Helpers"
             Background="{StaticResource AppBackgroundBrush}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <helpers:IsZeroConverter x:Key="IsZeroConverter" />
    </UserControl.Resources>

    <Grid>
        <TabControl BorderThickness="0,1,0,0">
            <TabItem Header="Dashboard">
                <Grid Margin="24">
                    <StackPanel>
                        <Border Style="{StaticResource CardStyle}" Padding="20" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="Solver Status" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                                <TextBlock Text="{Binding StatusMessage}" Foreground="#4B5563" Margin="0,0,0,15"/>
                                <ProgressBar Height="15" Value="{Binding JobProgress}" Maximum="100" 
                                             IsIndeterminate="{Binding JobProgress, Converter={StaticResource IsZeroConverter}}"/>
                                <Button Content="Start Temporal Solve" Command="{Binding RunSolveAsyncCommand}" 
                                        Margin="0,15,0,0" HorizontalAlignment="Left" Width="200" Style="{StaticResource PrimaryButtonStyle}"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Header="Settings">
                <Grid Margin="24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Border Style="{StaticResource CardStyle}" Padding="10">
                        <DataGrid ItemsSource="{Binding Settings}" AutoGenerateColumns="False" CanUserAddRows="False" GridLinesVisibility="Horizontal">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Configuration Key" Binding="{Binding Key}" Width="*" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="150"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                    <Button Grid.Row="1" Content="Export Settings (XLSX)" Command="{Binding ExportSettingsAsyncCommand}" 
                            HorizontalAlignment="Right" Margin="0,16,0,0" Width="180" Height="35"/>
                </Grid>
            </TabItem>

            <TabItem Header="Results">
                <Grid Margin="24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Border Style="{StaticResource CardStyle}" Padding="10">
                        <DataGrid ItemsSource="{Binding Results}" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="All">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Teacher" Binding="{Binding Teacher}" Width="*"/>
                                <DataGridTextColumn Header="Class" Binding="{Binding Class}" Width="*"/>
                                <DataGridTextColumn Header="Room" Binding="{Binding Room}" Width="100"/>
                                <DataGridTextColumn Header="Day" Binding="{Binding Day}" Width="100"/>
                                <DataGridTextColumn Header="Block" Binding="{Binding Block}" Width="60"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                    <Button Grid.Row="1" Content="Export Results (XLSX)" Command="{Binding ExportResultsAsyncCommand}" 
                            HorizontalAlignment="Right" Margin="0,16,0,0" Width="180" Height="35"/>
                </Grid>
            </TabItem>
        </TabControl>

        <Grid Background="#44000000" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
            <Border Background="White" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="30" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="{Binding TemporalStatus}" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <ProgressBar IsIndeterminate="True" Width="200" Height="10" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>